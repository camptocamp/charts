apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "secret.fullname" . }}-test-connection"
  labels:
{{ include "secret.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "secret.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
