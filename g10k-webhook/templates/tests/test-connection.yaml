apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "g10k-webhook.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "g10k-webhook.name" . }}
    helm.sh/chart: {{ include "g10k-webhook.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "g10k-webhook.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
