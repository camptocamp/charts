apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ekorre.fullname" . }}-test-connection"
  labels:
{{ include "ekorre.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ekorre.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
