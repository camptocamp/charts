apiVersion: v1
kind: Service
metadata:
  name: {{ include "common-build-dockerfile.fullname" . }}
  labels:
{{ include "common-build-dockerfile.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "common-build-dockerfile.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
