# Default values for imagestream.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: debian
  tag: stable
  pullPolicy: IfNotPresent
