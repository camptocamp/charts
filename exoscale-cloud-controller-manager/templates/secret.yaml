---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "exoscale-cloud-controller-manager.fullname" . }}-credentials
  labels:
{{ include "exoscale-cloud-controller-manager.labels" . | indent 4 }}
stringData:
  api-key: {{ .Values.exoscale.api-key | quote }}
  api-secret: {{ .Values.exoscale.api-secret | quote }}
  api-endpoint: {{ .Values.exoscale.api-endpoint | quote }}
