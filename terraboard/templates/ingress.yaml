{{- if .Values.ingress.enabled -}}
{{- $fullName := include "terraboard.fullname" . -}}
{{- $mainPort := .Values.service.port -}}
{{- $swaggerPort := .Values.swagger.service.port -}}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
{{ include "terraboard.labels" . | indent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
{{- if .Values.ingress.tls }}
  tls:
  {{- range .Values.ingress.tls }}
    - hosts:
      {{- range .hosts }}
        - {{ . | quote }}
      {{- end }}
      secretName: {{ .secretName }}
  {{- end }}
{{- end }}
  rules:
  {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
        {{- range .mainPaths }}
          - path: {{ . }}
            backend:
              serviceName: {{ $fullName }}
              servicePort: {{ $mainPort }}
        {{- end }}
        {{- range .swaggerPaths }}
          - path: {{ . }}
            backend:
              serviceName: {{ $fullName }}-swagger
              servicePort: {{ $swaggerPort }}
        {{- end }}
  {{- end }}
{{- end }}
